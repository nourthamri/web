{# <h1>Add Author <h1>
{{ form(form)}} #}
{% extends 'base.html.twig' %}

{% block title %}Add a Book{% endblock %}

{% block body %}
    <h1>Add a New Book</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
       
    {{ form_end(form) }}
{% endblock %}
